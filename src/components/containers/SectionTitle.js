const SectionTitle = ({ pageName, title }) => {
  return (
    <div className="left">
      <h3 className="text-[22px] font-bold font-psych">{title}</h3>
    </div>
  );
};
export default SectionTitle;
